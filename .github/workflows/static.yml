<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Perfect Stitch Tailor</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "San Francisco", "Segoe UI", Roboto, sans-serif;
      background: linear-gradient(145deg, #f6f7fb, #e8eaed);
    }
    .glass {
      backdrop-filter: blur(14px);
      background: rgba(255,255,255,0.6);
      box-shadow: 0 8px 32px rgba(31,38,135,0.1);
    }
  </style>
</head>
<body class="text-gray-800">

  <!-- Navbar -->
  <nav class="fixed top-0 left-0 w-full glass z-50">
    <div class="max-w-6xl mx-auto flex justify-between items-center px-6 py-4">
      <h1 class="text-2xl font-semibold text-gray-900">Perfect Stitch</h1>
      <div class="space-x-6 hidden md:flex">
        <a href="#home" class="hover:text-blue-600">Home</a>
        <a href="#services" class="hover:text-blue-600">Services</a>
        <a href="#booking" class="hover:text-blue-600">Book</a>
        <a href="#contact" class="hover:text-blue-600">Contact</a>
      </div>
    </div>
  </nav>

  <!-- Hero -->
  <section id="home" class="pt-32 pb-20 text-center px-6">
    <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Tailored for Perfection</h2>
    <p class="text-gray-600 max-w-xl mx-auto mb-8">
      Custom stitching, alterations & design — made to fit your lifestyle.  
      Experience modern tailoring at its finest.
    </p>
    <a href="#booking" class="px-6 py-3 bg-blue-600 text-white rounded-2xl shadow-lg hover:bg-blue-500 transition">Book Now</a>
  </section>

  <!-- Services -->
  <section id="services" class="bg-white/50 backdrop-blur-md py-16 px-6">
    <div class="max-w-6xl mx-auto text-center">
      <h3 class="text-3xl font-semibold mb-10">Our Services</h3>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="glass rounded-3xl p-6">
          <h4 class="text-xl font-semibold mb-2">👕 Custom Stitching</h4>
          <p class="text-gray-600">Perfect-fit shirts, pants, dresses, and bridal wear made to measure.</p>
        </div>
        <div class="glass rounded-3xl p-6">
          <h4 class="text-xl font-semibold mb-2">✂️ Alterations</h4>
          <p class="text-gray-600">Resize, repair, or restyle your existing clothes quickly & professionally.</p>
        </div>
        <div class="glass rounded-3xl p-6">
          <h4 class="text-xl font-semibold mb-2">🧵 Uniforms & Embroidery</h4>
          <p class="text-gray-600">Bulk orders and personalized embroidery for schools & corporates.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Booking -->
  <section id="booking" class="py-16 px-6">
    <div class="max-w-lg mx-auto glass rounded-3xl p-8 text-center">
      <h3 class="text-3xl font-semibold mb-6">Book Your Stitching</h3>
      <input id="name" type="text" placeholder="Full Name" class="w-full border border-gray-300 rounded-xl px-4 py-3 mb-3 focus:ring-2 focus:ring-blue-500 outline-none" required>
      <input id="phone" type="tel" placeholder="Phone Number" class="w-full border border-gray-300 rounded-xl px-4 py-3 mb-3 focus:ring-2 focus:ring-blue-500 outline-none" required>
      <select id="service" class="w-full border border-gray-300 rounded-xl px-4 py-3 mb-3 focus:ring-2 focus:ring-blue-500 outline-none">
        <option>Select Service</option>
        <option>Custom Stitching</option>
        <option>Alteration</option>
        <option>Uniform Order</option>
      </select>
      <textarea id="message" placeholder="Describe your request..." class="w-full border border-gray-300 rounded-xl px-4 py-3 mb-4 focus:ring-2 focus:ring-blue-500 outline-none"></textarea>

      <!-- Photo Upload -->
      <input id="photo" type="file" accept="image/*" class="w-full border border-gray-300 rounded-xl px-4 py-3 mb-4 focus:ring-2 focus:ring-blue-500 outline-none">
      <p class="text-sm text-gray-500 mb-4">Upload your design or fabric photo (optional)</p>

      <button onclick="sendWhatsApp()" class="w-full bg-blue-600 text-white py-3 rounded-xl hover:bg-blue-500 transition">Send on WhatsApp</button>

      <div class="mt-6">
        <p class="text-gray-700 mb-2">Or Pay in Advance 💳</p>
        <a href="upi://pay?pa=yourupiid@oksbi&pn=Perfect Stitch&am=500&cu=INR" class="bg-green-600 text-white px-4 py-2 rounded-xl hover:bg-green-500 transition">Pay ₹500 (UPI)</a>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <footer id="contact" class="py-10 text-center bg-gray-100 text-gray-600">
    <p class="text-lg">📍 Nelamangala, Bangalore | 📞 +91 6361632755</p>
    <p class="mt-2">Follow us: 
      <a href="#" class="text-blue-600 hover:underline">Instagram</a> • 
      <a href="#" class="text-blue-600 hover:underline">WhatsApp</a>
    </p>
    <p class="text-sm mt-4">© 2025 Perfect Stitch Tailor</p>
  </footer>

  <script>
    function generateOrderID() {
      const timestamp = Date.now().toString().slice(-5);
      const random = Math.floor(Math.random() * 1000);
      return timestamp + random;
    }

    async function sendWhatsApp() {
      const name = document.getElementById("name").value.trim();
      const phone = document.getElementById("phone").value.trim();
      const service = document.getElementById("service").value;
      const msg = document.getElementById("message").value.trim();
      const photo = document.getElementById("photo").files[0];

      if (!name || !phone || service === "Select Service") {
        alert("Please fill all required fields.");
        return;
      }

      const orderID = generateOrderID();
      let imageUrl = "";

      if (photo) {
        try {
          const formData = new FormData();
          formData.append("image", photo);
          const res = await fetch("https://api.imgbb.com/1/upload?key=2c45fc3797b9c028d427d5a83d13a268", {
            method: "POST",
            body: formData
          });
          const data = await res.json();
          if (data && data.data && data.data.url) imageUrl = data.data.url;
        } catch (err) {
          console.warn("Image upload failed: " + err.message);
        }
      }

      const whatsappNumber = "6361632755";
      const text = `Hello Perfect Stitch! 👋%0A%0AOrder ID: ${orderID}%0AName: ${name}%0APhone: ${phone}%0AService: ${service}%0ADetails: ${msg}%0A${imageUrl ? "📸 Image: " + imageUrl : ""}`;
      window.open(`https://wa.me/${whatsappNumber}?text=${text}`, "_blank");

      alert(`✅ Order Sent! Your Order ID: ${orderID}\nPlease save it for reference.`);
    }
  </script>

</body>
</html>
